# Админ-панель MasterProfi

Админ-панель для управления приложением MasterProfi.

## 🚀 Запуск админ-панели отдельно от бэкенда

Админ-панель запускается на **порту 3001** отдельно от основного бэкенда (который работает на порту 3000).

### Быстрый запуск:

#### Linux/Mac:
```bash
cd backend/admin-panel
chmod +x start-admin.sh
./start-admin.sh
```

#### Windows (PowerShell):
```powershell
cd backend\admin-panel
.\start-admin.ps1
```

#### Вручную:
```bash
cd backend/admin-panel

# Установка зависимостей (если еще не установлены)
npm install

# Сборка проекта
npm run build

# Запуск через PM2
pm2 start npm --name "admin-panel" -- run serve
pm2 save
```

### Управление через PM2:

```bash
# Просмотр статуса
pm2 list

# Просмотр логов
pm2 logs admin-panel

# Перезапуск
pm2 restart admin-panel

# Остановка
pm2 stop admin-panel

# Удаление из PM2
pm2 delete admin-panel
```

## 🌐 Доступ

После запуска админ-панель будет доступна по адресу:
- **http://212.74.227.208:3001/admin** (или ваш IP/домен)

## 📝 Разработка

Для разработки с hot-reload:
```bash
npm run dev
```

Админ-панель будет доступна на `http://localhost:3001/admin`

## ⚙️ Конфигурация

- **Порт**: 3001 (настраивается в `vite.config.js`)
- **Базовый путь**: `/admin/` (настраивается в `vite.config.js`)
- **API URL**: Настраивается через переменную окружения `VITE_API_URL` или по умолчанию `http://212.74.227.208:3000/api`

## 🔄 Обновление

При обновлении кода используйте скрипт `update-server.sh` из корня `backend/`, который автоматически:
1. Остановит админ-панель
2. Обновит код из Git
3. Соберет проект
4. Перезапустит админ-панель

Или вручную:
```bash
cd backend/admin-panel
git pull
npm install
npm run build
pm2 restart admin-panel
```
